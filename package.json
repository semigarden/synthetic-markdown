{
    "private": true,
    "workspaces": [
        "app",
        "dev/*",
        "packages/*"
    ],
    "scripts": {
        "dev": "npm -w dev/syntxt-dev-react run dev",
        "build": "npm -ws run build",
        "typecheck": "npm -ws run typecheck",
        "lint": "npm -ws run lint"
    }
}
